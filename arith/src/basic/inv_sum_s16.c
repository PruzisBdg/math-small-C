/*-----------------------------------------------------------------------------------------
|
|                      1
|     returns     -----------
|                 1/a  +  1/b
|
------------------------------------------------------------------------------------------*/

#include "arith.h"

PUBLIC S16 InvSumS16(S16 a, S16 b)
{
   if( a == 0 || b == 0 )        // singularity?
      { return 0; }              // returns 0
   else                          // else do calc
      { return ClipFloatToInt( 1.0/((1.0/a) + (1.0/b)) ); }
}

  
