/*-----------------------------------------------------------------------------------------
|
|  VecB_MeanDiffS16()
|
------------------------------------------------------------------------------------------*/

#include "arith.h"

PUBLIC S16 VecB_MeanDiff(S16 *v1, S16 *v2, U8 cnt)
{
   U8 c;
   S32 sum;
   
   for( c = 0, sum = 0; c < cnt; c++ )
   {
      sum += v1[c];
      sum -= v2[c];
   }
   return (S16)(sum/cnt);
}

 
