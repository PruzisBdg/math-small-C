/*-----------------------------------------------------------------------------------------
|
|                      1
|     returns     -----------
|                 1/a  +  1/b
|
------------------------------------------------------------------------------------------*/

#include "arith.h"

PUBLIC U8 InvSumU8(U8 a, U8 b)
{
   if( a == 0 || b == 0 )        // singularity?
      { return 0; }              // returns 0
   else                          // else do calc... 'MAX_S16/2' avoids overrange
      { return (MAX_S16/2) / (( (MAX_S16/2)/a) + ( (MAX_S16/2)/b)); }
}

 
