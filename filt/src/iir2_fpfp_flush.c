/*-------------------------------------------------------------------------------------------------
|
|  IIRFF2_Flush
|
|  Flush 'f' with 'n'.  N.B See IIRF2_Flush() for 'n' = integer
|
-------------------------------------------------------------------------------------------------*/

#include "iirfilt_fp.h"

PUBLIC void IIRFF2_Flush( S_IIRF2 *f, float n ) 
{
	f->buf[0] = n;
	f->buf[1] = n;
   f->tapIdx = 0;
}

// ------------------------------------- eof ----------------------------------------- 
